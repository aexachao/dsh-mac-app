# Changelog

本文件记录 Harness 的版本变更。Release 构建时由 GitHub Actions 读取（`v*` tag 对应的条目），并附加 git 提交生成 release notes。

## [0.2.1] - 2026-08-29

### 安全
- 日志脱敏：密钥、`Cookie`/`Authorization` 头、URL 中的 userinfo、OAuth 回调里的 `code`/`token` 等在进入日志缓冲区前替换为 `****`，日志可直接贴到 issue；`exit code: 1`、`--port 3080` 这类诊断信息保持原样
- 重启不再盲杀端口占用者：只终止命令行可确认为 dsh 启动入口的残留进程，其它进程一律保持不动并记入日志
- 只有核对端口占用者确实是 dsh、且已能响应 HTTP 时才接管既有服务，避免把用户其它本地服务的页面加载进应用窗口

### 修复
- 端口不再假定为 3080：应用先挑一个确实可绑定的端口，再用 `--port` 显式传给 dsh；首选端口被占用时向后退让（最多扫 32 个）
- 端口释放判定改用 `bind()` 而非 HTTP 探测——dsh 从开始监听到能响应 HTTP 有数十秒窗口，HTTP 探测会把正在初始化的服务误判成端口已空闲

### 优化
- 日志时间戳格式化器改为复用，日志高峰时不再每行新建 `DateFormatter`

## [0.2.0] - 2026-08-22

### 新增
- GitHub Actions 自动构建与发布（`v*` tag 触发）：universal 双架构（Apple Silicon + Intel）打包、Release 创建、变更日志生成
- 构建脚本支持版本注入（`VERSION`/`BUILD` 环境变量）与架构选择（`ARCHS`）

## [0.1.1] - 2026-08-21

### 修复
- 服务端残留进程导致配置变更不生效：重启服务（⌘⇧R）现在会等待端口释放并自动清理占用进程
- 日志面板在界面就绪后无法收起：面板自带关闭按钮（⌘⇧L 快捷键保留）

### 优化
- 启动提速：跳过 npx 网络检查（缓存存在时直接运行，1-2 秒就绪）；端口可连即加载界面，不等完整初始化
- 缓解 WebView 流式渲染输入延迟：防 App Nap 降频 + layer 合成
- 菜单新增「在浏览器中打开」（⌘⇧O）

## [0.1.0] - 2026-08-19

### 新增
- Harness 首个版本：DeepSeek dsh web 的 macOS 原生包装
  - 打开应用自动启动服务、退出自动停止（直接子进程，无孤儿）
  - 启动/运行/失败三态界面 + 实时日志面板
  - 隐藏标题栏沉浸式界面、深色窗口背景
  - 中文菜单（日志 / 重启服务 / 重新加载）
  - 黑底白 logo 应用图标
