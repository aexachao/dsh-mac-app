# Changelog

本文件记录 Harness 的版本变更。Release 构建时由 GitHub Actions 读取（`v*` tag 对应的条目），并附加 git 提交生成 release notes。

## [0.3.6] - 2026-08-30

### 变更
- **窗口顶部不再有我们自己的一条导航栏**：网页直接铺到 y=0，红绿灯下方那一段由页面自己让位（给 dsh 的挂载点 `#root` 加一段 `padding-top`，高度按标题栏实测而不是写死 32pt）。让位出来的横带按左右两栏分别取色——从让位高度下方的左右两点各测一次页面实际背景色，画成「左段侧栏色、右段会话区色」，因此换主题、折叠侧栏都自动跟上；测不到就退回单色，跟改动之前完全一样。原来那块露出的是窗口背景色，跟 dsh 的两种底色都不同，看着就像产品上面压了一条属于我们的标题栏
- 红绿灯右边那条现在归网页所有，鼠标事件不再进标题栏，所以补了一层透明拖拽条：拖动照旧，双击跟随系统的「双击标题栏」设置（缩放 / 最小化 / 无）
- **关闭窗口不再退出应用、不再停止服务**。agent 的任务跑在 dsh 服务端，窗口只是它的客户端；此前关窗把服务一起停掉，等于把用户正在跑的活打断在半路——那正是网页端关标签页的下场，也正是原生外壳本该避免的。现在关窗只收起界面，应用留在 Dock 里、服务继续跑；点 Dock 图标或「窗口 → 显示主窗口」把它叫回来，⌘Q（或菜单退出、Dock 右键退出）才真退出，那条路径照旧收整棵进程树。窗口菜单同时补上「关闭窗口」（⌘W）和「显示主窗口」两条：⌘W 从「危险操作」变成了日常操作，而关过窗之后菜单里必须找得回来
- 关窗时往日志里留一行「服务继续在后台运行」：不写的话，用户过后发现 node 还在跑，只会以为应用没退干净
- **dmg 挂载后 Finder 里显示我们自己的图标**：往映像根目录放一份 `.VolumeIcon.icns`，再给卷根目录置上 `kHasCustomIcon` 标志位。dmg **文件本身**的图标改不了——文件级自定义图标存在资源分支／扩展属性里，HTTP 下载只搬字节，那份元数据到不了用户机器上，所以下载下来的 `.dmg` 一定是系统的通用磁盘映像图标，能带过去的品牌化只有卷这一层

### 修复
- **应用不再有机会在启动时整个冻住**。安全模式的插件清单扫描此前跑在主线程上，而 dsh 的插件常以符号链接指向开发仓库、仓库又多半在 `~/Documents` 这类受 macOS 隐私保护的目录里——没授权时读那条路径不是报错、是 `open()` 永远不返回。于是整个应用界面卡死，日志一行异常都没有。现在扫描挪到后台并带 3 秒时限（新增 `Deadline`：卡住的线程被放弃，调用方照样往下走；`Task.cancel()` 在这里无效，阻塞在内核 `open()` 里的线程观察不到取消）；超时按正常模式启动，而不是写一份「没有插件要停用」的假清单
- **启动时先给插件路径做一次体检**，把「链到隐私保护目录、此刻确实读不动」的插件连同处置办法写进日志（失败态会自动展开日志侧栏）。枚举只 `readlink`、不打开，本身不会被拦；真正探测那一步才需要时限。这几行替掉的是「界面停在启动中、九十秒后报无响应、要靠 `sample` 抓 node 调用栈才查得出病根」
- **补上三条隐私授权用途说明**（`NSDocumentsFolderUsageDescription` / `Desktop` / `Downloads`，中英各一份 `InfoPlist.strings`）。子进程继承本应用的授权，所以系统弹的是我们的窗、念的是我们的理由；没有这三个键就没有理由可念

## [0.3.5] - 2026-08-30

### 变更
- **分发包从 zip 改为 dmg**，只发 dmg。zip 解压出来的应用仍带隔离标记，在原地双击会触发 macOS 的 App Translocation——应用被搬到 `/var/folders` 下的只读临时挂载点执行，路径每次启动都不一样，于是单实例锁换了位置、文件夹访问授权反复弹窗，退出后残留的 dsh 子孙进程还会一直占着那个挂载点。dmg 里放一个指向「应用程序」的符号链接，用户先拖过去再打开，这一类问题从源头消失。dmg 本身也单独签名、公证、装订——应用里的票据不解除容器的隔离标记

### 修复
- **退出应用不再留下 dsh 插件派生的孙进程**：此前只终止直接子进程（那个 node），插件自己 spawn 的常驻进程（实测 dsh-doctor 的 supervisor 连 SIGTERM 都不理）在 node 死后被 launchd 收养，活过应用退出——继续占着端口，也一直占着应用所在的挂载点，从 dmg 启动时连卷都卸不掉。现在退出前先抓下整棵子孙树再动手（父进程一退出，子孙的 PPID 全变成 1，那棵树就再也认不出来），先 SIGTERM 全体、1.5 秒后对仍在的补 SIGKILL。判据是父子关系而不是命令行特征：那些进程是我们自己派生出来的，这件事本身就是所有权证明
- **菜单诊断日志不再无上限增长**：守护 Timer 每 3 秒把菜单结构写一遍，写的又是同一份没变过的结构，一台开着不动的机器实测把日志堆到 55 MB——真正有用的那几行反而被埋掉。现在周期快照只在结构变了时写，事件（重建、被覆盖）一律写；文件封顶 256 KB，到顶从头重写。位置也从 `/tmp`（所有用户共享的固定路径）移到 `~/Library/Logs/Harness/menu-state.log`
- **WebView 的导航策略此前一次都没生效过**：`decidePolicyFor` 的 `decisionHandler` 少了 `@MainActor @Sendable`，Swift 就不会把这个方法暴露给 Objective-C，WebKit 用 `respondsToSelector:` 探测不到便按默认策略放行——外链因此一直在应用窗口里打开，而不是交给系统浏览器。六个测试只测那个纯函数，全过也测不出委托根本没被调用；现在由 selector 暴露测试钉住
- 公证脚本的两条报错分支会先把自己搞崩：变量紧跟全角标点（`$APP，`、`（$REQUEST_ID）`）在 macOS 自带的 bash 3.2 下会把标点并进变量名，`set -u` 当场中止。其中一条正是公证失败后去拉 Apple 逐条日志的那行——最需要诊断的时候什么都拿不到
- 签名降级不再只留在 CI 日志里：ad-hoc 或未公证的构建会把「首次打开需要右键 →「打开」」写进 release notes。此前流水线注释声称会这么做，实际并没有，用户只会看到一个打不开的应用
- `CHANGELOG.md` 缺少本次版本条目时，发布流水线给出 `::warning::`：版本号由 CI 自动 +1，手写条目对不上时 release notes 会静悄悄地只剩提交列表

### 安全
- 跟随上游的 PR 标题与提交信息改从环境变量取值，不再把 npm / nodejs.org 返回的版本字符串直接展开进 shell 脚本文本（同一份文件里的 diff 早已这么处理，这两处漏了）

## [0.3.4] - 2026-08-30

### 修复
- CI 导入签名证书时显式指定 `-f pkcs12`：`security import` 按文件扩展名猜格式，而脚本用的临时文件没有后缀，导致证书导入必然失败、发布流水线在几秒内中止

## [0.3.3] - 2026-08-30

### 修复
- CI 的 PKCS#12 兼容回退改用 Bash 3.2 安全的参数写法，并在证书导入失败时立即中止，避免静默退回 ad-hoc 签名
- npm 安装内置 DSH 运行时的 Node.js 堆上限提高到 4 GB，避免 arm64 runner 在约 2 GB 默认上限处内存溢出

## [0.3.2] - 2026-08-30

### 修复
- PKCS#12 兼容回退会检测当前 `openssl` 是否支持 `-legacy`，同时兼容本机 OpenSSL 3 与 GitHub macOS runner 的 LibreSSL

## [0.3.1] - 2026-08-30

### 修复
- CI 导入 Developer ID 证书失败时，用 OpenSSL 重新封装为兼容的 PKCS#12 格式后重试，解决 GitHub macOS runner 报 `SecKeychainItemImport: Unknown format in import`

## [0.3.0] - 2026-08-29

### 新增
- **内置运行时**：定版的 Node.js（24.20.0）与 dsh（0.1.1-rc.2）随应用分发，下载安装即可用。此前全新机器的第一次启动要等 `npx` 拉 283 MB 依赖树（实测约 14 分钟，网络稍差就直接失败），现在启动路径上一次网络请求都没有。版本锁在 `runtime-pins.json`，node 压缩包按 SHA256 校验后才打进包，对不上就中止构建
- **逃生开关「改用本机安装的 dsh」**（设置面板）：内置的 dsh 与 `~/.dsh` 里的配置对不上时，不必等我们发版。默认关闭，绝大多数用户不需要知道它存在
- **失败分类新增「内置的 dsh 版本对不上」**：同一条配置报错，在内置运行时下不再说「你的配置写错了」，而是说明这是我们发的版本读不懂被更新版 dsh 迁移过的文件，并把逃生开关作为主按钮推到面前（此时「重试」注定再失败一次）；用本机运行时启动时仍按原来的「配置格式不符」处理，直接在 Finder 中定位到出错的文件
- **Developer ID 签名 + 公证**：分发包经 Apple 公证并装订票据，用户不再需要右键打开或绕过 Gatekeeper。所有嵌套 Mach-O 由内向外逐个签名并开启加固运行时；JIT、未签名可执行内存、关闭库校验三条豁免只给内置的 node（V8 的硬需求 + 让用户自己装的第三方原生插件能加载），应用本体没有任何豁免。本地构建同样开加固运行时，避免「本地能跑、公证版起不来」这类差异等到发布后才暴露
- **CI 每天跟随上游**：`follow-upstream.yml` 检查 npm 与 nodejs.org，`runtime-pins.json` 有变化就开 PR（带 diff 与人工验证清单）。捆绑把「版本漂移」换成了「我们负责跟随」，没有这条线用户装到的就永远是发版那天的版本。dsh 跟 npm `latest`；node 只在锁定的大版本内跟随——跨大版本会换掉 `NODE_MODULE_VERSION`，树里每个预构建 `.node` 都要重来，那是移植而不是升级，必须由人决定

### 变更
- 应用 Bundle ID 从开发期的 `local.harness.app` 改为正式的 `com.chrisli.dsh-desktop`
- **发布改为按架构出两个包**（`apple-silicon` / `intel`），不再出 universal。dsh 的依赖树带平台专属的原生模块（`@img/sharp-darwin-*`、`@koromix/koffi-darwin-*`、`node-pty` 预构建），一份 `node_modules` 只能属于一个架构，universal 装不进两份互斥的树——这不是没做，而是做不到。下载时请选对架构，装错了起不来
- 压缩包因此变大到约 115 MB（内置运行时未压缩约 384 MB）。换来的是首次启动不下载、不联网、不受本机 Node 环境影响
- `README.md` 的「依赖」一节：Node.js 不再是使用前提，只有打开逃生开关时才需要

### 修复
- 自动发布会把仓库 Secrets 传给复用的发布工作流；CI 只有在 App Store Connect API 密钥的 Key ID、Issuer ID 与私钥都齐全时才启用公证；本机关闭 Gatekeeper 时，公证脚本会明确警告 `spctl` 无法完成有效复核

## [0.2.1] - 2026-08-29

### 新增
- 日志落盘到 `~/Library/Logs/Harness/`（`harness-<日期>[.<分段>].log`）：单文件 2 MB 分段、目录总量封顶 16 MB、单行 8 KB 按 UTF-8 边界截断；清理只针对本应用写出的文件，目录里其它文件既不计入总量也绝不删除
- 「文件 → 导出诊断信息…」一键生成可直接贴进 issue 的报告：版本、macOS、架构、界面语言、解析到的 Node 与 dsh 入口、服务状态与端口、日志文件清单和最近 200 行日志；导出前整份再过一遍脱敏
- 「文件 → 打开日志目录」在 Finder 中定位落盘日志
- 失败态可操作：启动失败不再只显示一句英文报错，而是分类给出「是什么 / 为什么 / 怎么办」——缺 Node 给下载入口、配置文件格式不符直接在 Finder 中定位到出错的文件、端口耗尽说明扫过的范围、异常退出附上日志里的最后一条报错并提供导出诊断
- 启动健康判定：端口能连上只说明 dsh 开始监听，判定要求页面确实加载完成、且进程从启动起存活满 20s（页面 60s 未加载完成即判失败），结果与连续失败次数一并落盘，为后续的安全模式提供依据
- 安全模式：连续 3 次启动异常后自动以安全模式启动，临时停用全部第三方插件（保留 `@deepseek-ai/` 的第一方插件），界面常驻横幅说明现在少了什么、可查看停用清单并一键退出；菜单「以安全模式重启」也可手动进入。插件清单静态扫描 profile 目录得来，不需要 dsh 能启动；停用只通过应用自己目录下的 `--patch` overlay 叠加，**绝不写入 `~/.dsh`**，删掉 overlay 即恢复原状。诊断报告一并记录安全模式状态与停用清单
- 单实例锁：重复打开不再起第二个服务。macOS 只拦「同一个 bundle 再次被打开」，`open -n`、`swift run`、以及 `dist/` 与 `~/Applications/` 两份拷贝都能绕过去，而两个实例意味着两个 dsh 争抢同一份 `~/.dsh` 状态、两份日志交替写进同一个目录。锁用 `flock`（`~/Library/Application Support/Harness/instance.lock`）而非 pid 文件，随进程结束自动释放，崩溃后不会留下解不开的死锁；抢不到锁就把已有窗口叫到前台后自己退出（找不到可激活的实例时弹窗说明，而不是静悄悄地消失）
- 界面英文文案补齐：切到 English 后状态栏、启动提示、日志面板、安全模式横幅、设置面板、单实例提示全部随之切换，不再残留中文。文案集中在一张双语表里，漏写一种语言编译不过，英文位抄成中文、两种语言占位符对不上则测试不过

### 安全
- 日志脱敏：密钥、`Cookie`/`Authorization` 头、URL 中的 userinfo、OAuth 回调里的 `code`/`token` 等在进入日志缓冲区前替换为 `****`，日志可直接贴到 issue；`exit code: 1`、`--port 3080` 这类诊断信息保持原样
- 重启不再盲杀端口占用者：只终止命令行可确认为 dsh 启动入口的残留进程，其它进程一律保持不动并记入日志
- 只有核对端口占用者确实是 dsh、且已能响应 HTTP 时才接管既有服务，避免把用户其它本地服务的页面加载进应用窗口

### 修复
- 不再让 dsh 自己弹浏览器：dsh 从 `0.1.1-rc.2` 起默认打开系统浏览器，而应用窗口本身就是那个页面，启动一次白送一个标签页。启动参数固定带上 `--no-open`
- 首次安装 dsh 不再必然超时：`npx` 装 `@deepseek-ai/dsh` 连同 web profile 的依赖树是 283 MB，实测约 14 分钟，原先 180 秒的上限连依赖解析都走不完——也就是说全新机器的第一次启动一定失败在这一步。上限改为 20 分钟，并在日志里提前说明量级与预计耗时，免得用户以为卡死去强杀（装到一半的 npx 缓存是坏的）
- 端口不再假定为 3080：应用先挑一个确实可绑定的端口，再用 `--port` 显式传给 dsh；首选端口被占用时向后退让（最多扫 32 个）
- 端口释放判定改用 `bind()` 而非 HTTP 探测——dsh 从开始监听到能响应 HTTP 有数十秒窗口，HTTP 探测会把正在初始化的服务误判成端口已空闲
- 切换语言后的自重启不再固定 `sleep 1`，改为轮询旧进程是否真的退出（上限 10 s）再 `open`：旧进程退得慢一点，新实例就会撞上还没释放的单实例锁，表现为「切换语言后应用直接没了」

### 优化
- 日志时间戳格式化器改为复用，日志高峰时不再每行新建 `DateFormatter`

## [0.2.0] - 2026-08-22

### 新增
- GitHub Actions 自动构建与发布（`v*` tag 触发）：universal 双架构（Apple Silicon + Intel）打包、Release 创建、变更日志生成
- 构建脚本支持版本注入（`VERSION`/`BUILD` 环境变量）与架构选择（`ARCHS`）

## [0.1.1] - 2026-08-21

### 修复
- 服务端残留进程导致配置变更不生效：重启服务（⌘⇧R）现在会等待端口释放并自动清理占用进程
- 日志面板在界面就绪后无法收起：面板自带关闭按钮（⌘⇧L 快捷键保留）

### 优化
- 启动提速：跳过 npx 网络检查（缓存存在时直接运行，1-2 秒就绪）；端口可连即加载界面，不等完整初始化
- 缓解 WebView 流式渲染输入延迟：防 App Nap 降频 + layer 合成
- 菜单新增「在浏览器中打开」（⌘⇧O）

## [0.1.0] - 2026-08-19

### 新增
- Harness 首个版本：DeepSeek dsh web 的 macOS 原生包装
  - 打开应用自动启动服务、退出自动停止（直接子进程，无孤儿）
  - 启动/运行/失败三态界面 + 实时日志面板
  - 隐藏标题栏沉浸式界面、深色窗口背景
  - 中文菜单（日志 / 重启服务 / 重新加载）
  - 黑底白 logo 应用图标
